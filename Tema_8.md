# Тема 8. Основы объектно-ориентированного программирования
Отчет по Теме #8 выполнил
- Шенол Эмир Хан
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | - |
| Задание 2 | + | - |
| Задание 3 | + | - |
| Задание 4 | + | - |
| Задание 5 | + | - |
| Задание 6 | - | - |
| Задание 7 | - | - |
| Задание 8 | - | - |
| Задание 9 | - | - |
| Задание 10 | - | - |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Создайте класс “Car” с атрибутами производитель и модель. Создайте объект этого класса. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями.

```python
class Car:
    def __init__(self, make, model):
        self.make = make
        self.model = model

my_car = Car("Toyota", "Corolla")
```
### Результат
![Mеню](https://github.com/EmirHan5534/Software_Engineering/blob/%D0%A2%D0%B5%D0%BC%D0%B0_8/pic/Lab8_1.png)

## Выводы
В данном коде:
1. class Car: — начинается описание нового класса под названием "Car".
2. def __init__(self, make, model): — это специальный метод-конструктор, инициализирующий каждый новый объект этого класса.
3. self.make = make — экземпляру класса присваивается свойство "make", которое получает значение из первого аргумента.
4. self.model = model — экземпляру класса присваивается свойство "model", которое получает значение из второго аргумента.
5. my_car = Car("Toyota", "Corolla") — на основе класса создается конкретный объект с заданными характеристиками.

## Лабораторная работа №2
### Дополните код из первого задания, добавив в него атрибуты и методы класса, заставьте машину “поехать”. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
class Car:
    def __init__(self, make, model):
        self.make = make
        self.model = model

    def drive(self):
        print(f"Driving the {self.make} {self.model}")

my_car = Car("toyota", "Cororlla")
my_car.drive()
```
### Результат
![Меню](https://github.com/EmirHan5534/Software_Engineering/blob/%D0%A2%D0%B5%D0%BC%D0%B0_8/pic/Lab8_2.png)

## Выводы
В данном коде:
1. def drive(self): - определяет метод "drive", который может выполнять действия с объектом.
2. print(f"Driving the {self.make} {self.model}") - выводит сообщение с использованием атрибутов объекта.
3. my_car = Car("toyota", "Cororlla") - создает экземпляр (объект) класса Car с конкретными значениями атрибутов.
4. my_car.drive() - вызывает метод "drive" для объекта my_car, что приводит к выводу сообщения "Driving the toyota Cororlla".

## Лабораторная работа №3
### Создайте новый класс “ElectricCar” с методом “charge” и атрибутом емкость батареи. Реализуйте его наследование от класса, созданного в первом задании. Заставьте машину поехать, а потом заряжаться. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
from Lab8_2 import Car
class ElectroCar(Car):
    def __init__(self, make, model, battery_capacity):
        super().__init__(make,model)
        self.battery_capacity = battery_capacity

    def charge(self):
        print(f"Charging the {self.make} {self.model} with {self.battery_capacity} kWh")

my_electronic_car = ElectroCar("Tesla", "Model S", 75)
my_electronic_car.drive()
my_electronic_car.charge()
```
### Результат
![Меню](https://github.com/EmirHan5534/Software_Engineering/blob/%D0%A2%D0%B5%D0%BC%D0%B0_8/pic/Lab8_3.png)

## Выводы
В данном коде:
1. from Lab8_2 import Car — подключает класс Car из модуля Lab8_2 для использования в текущем файле.
2. def __init__(self, make, model, battery_capacity): — задает метод инициализации для класса ElectroCar с тремя входными параметрами.
3. self.battery_capacity = battery_capacity — добавляет объекту уникальное свойство "battery_capacity", характерное для электромобилей.
4. def charge(self): — вводит в класс новый метод "charge", отвечающий за функционал зарядки автомобиля.

## Лабораторная работа №4
### Реализуйте инкапсуляцию для класса, созданного в первом задании. Создайте защищенный атрибут производителя и приватный атрибут модели. Вызовите защищенный атрибут и заставьте машину поехать. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
class Car:
    def __init__(self, make, model):
        self._make = make # Защищенный атрибут
        self.__model = model # Приватный атрибут

    def drive(self):
        print(f"Driving the {self._make} {self.__model}")


my_car = Car("Toyota", "Corolla")
print(my_car._make) # Доступ к защищенному атрибуту
my_car.drive()
```
### Результат
![Меню](https://github.com/EmirHan5534/Software_Engineering/blob/%D0%A2%D0%B5%D0%BC%D0%B0_8/pic/Lab8_4.png)

## Выводы
В данном коде:
1. self._make = make — создает атрибут с модификатором доступа "protected" (защищенный), обозначаемый одним подчеркиванием, который указывает на внутреннее использование в классе и его потомках.
2. self.__model = model — создает атрибут с модификатором доступа "private" (приватный), обозначаемый двойным подчеркиванием, который предназначен для использования только внутри текущего класса.
3. print(my_car._make) - демонстрирует обращение к защищенному атрибуту объекта извне класса.

## Лабораторная работа №5
### Реализуйте полиморфизм создав основной (общий) класс “Shape”, а также еще два класса “Rectangle” и “Circle”. Внутри последних двух классов реализуйте методы для подсчета площади фигуры. После этого создайте массив с фигурами, поместите туда круг и прямоугольник, затем при помощи цикла выведите их площади. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height

    def area(self):
        return self.width * self.height

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius

    def area(self):
        return 3.14 * self.radius * self.radius

rect = Rectangle(5, 4) # Создан экземпляр класса Rectangle с шириной 5 и высотой 4
circle = Circle(3) # Создан экземпляр класса Circle с радиусом 3

print(rect.area()) # Вызов метода area() для прямоугольника и вывод результата
print(circle.area()) # Вызов метода area() для круга и вывод результата
```
### Результат
![Меню](https://github.com/EmirHan5534/Software_Engineering/blob/%D0%A2%D0%B5%D0%BC%D0%B0_8/pic/Lab8_5.png)

## Выводы
В данном коде:
1. class Rectangle(Shape): — объявляет класс Rectangle, который наследует свойства и методы от родительского класса Shape.
2. def __init__(self, width, height): — определяет конструктор класса Rectangle, принимающий параметры ширины и высоты.
3. self.width = width — создает атрибут width экземпляра класса и присваивает ему значение параметра width.
4. self.height = height — создает атрибут height экземпляра класса и присваивает ему значение параметра height.
5. def area(self): — определяет метод area() для расчета площади прямоугольника.
6. return self.width * self.height — возвращает результат вычисления площади прямоугольника.
7. class Circle(Shape): — объявляет класс Circle, который также наследует от класса Shape.
8. def __init__(self, radius): — определяет конструктор класса Circle, принимающий параметр радиуса.
9. self.radius = radius — создает атрибут radius экземпляра класса и присваивает ему значение параметра radius.
10. rect = Rectangle(5, 4) — создает экземпляр класса Rectangle с шириной 5 и высотой 4.
11. circle = Circle(3) — создает экземпляр класса Circle с радиусом 3.
12. circle = Circle(3) — создает экземпляр класса Circle с радиусом 3..
13. print(circle.area()) — вызывает метод area() для круга и выводит результат.
